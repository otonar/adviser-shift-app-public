# 変更履歴（CHANGELOG）

チームメンバー向けの変更まとめ。新しいものを上に追記する。
各エントリは「変更点」と、必要なら「対応（pull 以外に必要なこと）」を書く。
原則として `main` に反映した内容を記録する。

---

## 2026-06-12

### 変更点
- **スマホ下ナビの高さ改善**: 「シフト/役割/商品/設定」のボタンが縦に窮屈だったのを解消。タップ領域を拡大し、iPhone のホームバー（セーフエリア）にも対応。
- **シフトの並び順を改善**（希望提出画面・役割確認画面）:
  - 同じ日付内は**開始時刻の早い順**で表示。
  - **過ぎたシフトは下**へ、**次のシフトが一番上**に来るよう並べ替え（JST で日付が変わったら「過去」扱い）。
- PWA アイコン更新／`.vercel` を `.gitignore` に追加。

### 対応
- `git pull` のみ。**環境変数・DB スキーマの変更なし**、追加インストール不要。
- 本番（Vercel）は `main` へのマージで自動再デプロイ済み。
