# 変更履歴（CHANGELOG）

チームメンバー向けの変更まとめ。新しいものを上に追記する。
各エントリは「変更点」と、必要なら「対応（pull 以外に必要なこと）」を書く。
原則として `main` に反映した内容を記録する。

---

## 2026-09-25

### 変更点
- **公式 LINE のトークで、自分のシフトを聞けるようになりました**。トークに次の言葉を送ると返事が来ます。
  - 「**直近のシフト**」（「シフト」でも可）… 確定したこれからのシフトと役割（近い順に3件まで）
  - 「**未提出の枠**」（「未提出」でも可）… まだ希望を出していない枠と締切（「今日まで！」「あと2日」など）
  - 「**ヘルプ**」… 使い方
  - 返事やメニューのリンクは **LINE の中で開きます**（閉じればすぐトークに戻れます）。**最初の1回だけ LINE の中でログインが必要**で、その後はログインしたままになります。
  - **それ以外の言葉には返事をしません**（公式アカウントへの普通の問い合わせの邪魔をしないため）。
  - アプリの「設定」で **LINE 連携をしていない人には、連携の案内が返ります**。
- **公式 LINE のトーク画面の下にメニューが付きました**（トーク下部の「メニュー」で開閉）。
  - 「直近のシフト」「未提出の枠」… 押すとトークで返事が来ます（上の言葉を打つのと同じ）
  - 「希望を出す」「リンク集」… アプリのその画面が開きます
  - 出ていない人は、トークを一度閉じて開き直してください。
  - メニューの文言・色を変えるときは `scripts/setup-richmenu.mjs` を直して登録し直します（手順はファイル冒頭）。**公式アカウントマネージャーの画面では作らないでください**（設定が引き継げなくなるため）。

### 対応
- `git pull` のみ。データベースの変更はありません。
- LINE 側の設定（Webhook の有効化・自動応答メッセージのオフ）は済んでいます。**公式アカウントの設定を触る人は、Webhook をオフにしたり自動応答をオンに戻したりしないでください**（返事が来なくなる／定型文が毎回重なって返るようになります）。

---

## 2026-09-24（その3）

### 変更点
- **脱退した人がシフトに割り振られてしまう問題を直しました**。
  - これまでは、**すでに対象者になっている枠で「出られる」を出したあとに脱退すると、自動割り振りでそのまま選ばれていました**（本人はログインできず通知も届かないのに、名簿には名前が残る状態）。
  - **自動割り振りの候補から脱退者を外しました**。手動で追加するプルダウンにも出なくなります。
  - **管理画面に「脱退」の表示を追加しました**。提出状況と割り振りの一覧で名前の横に出ます（見出しにも「うち脱退◯名」）。
  - **確定して共有するときに警告が出ます**。割り振りに脱退者が残っていると名前を挙げて確認し、その人には通知を送りません（公開自体は止めません）。
  - 分析の「役割ごとの充足」で、脱退者の割り当てを「埋まっている」と数えないようにしました。**実際は欠員なのに足りているように見えていた**ためです。

### 対応
- `git pull` のみ。データベースの変更はありません。

---

## 2026-09-24（その2）

### 変更点
- **動作確認用に入れていたテストデータを、本番のデータベースから削除しました**。
  - 消えたもの: 「テスト_」で始まるメンバー12名と商品5件、確認用に作ったシフト枠12件（6〜9月のもの）、検証用アカウント3件、下書きのアンケート結果3件、およびそれらに紐づく希望提出・割り振り・通知ログ。
  - **残っているもの**: 実際のメンバー3名、商品2件（Dynabook・プリンタ）、**10/03（研修・共済）と 10/10（研修・学び/個別相談）のシフト枠**。

### 対応
- **⚠️ 10/03 と 10/10 の枠は、対象者を選び直してください**（管理画面 → シフト → 枠を開く → 対象者を編集）。この2枠に入っていた対象者と提出は、削除したテストアカウントのものだったため一緒に消えています。**対象者を設定するまで、誰も希望を出せません。**
- アプリの使い方は変わりません。`git pull` も不要です（データベース側の作業のため）。

---

## 2026-09-24

### 変更点
- **アプリのアイコンを正式版（桜とコンパスのイラスト）に差し替えました**。仮のアイコンから変わります。
  - **ホーム画面に追加したときのアイコンが欠けないようにしました**。Android はアイコンを丸く切り抜くので、そのままだと花びらの先が欠けます。切り抜き用に少し余白を足した画像を別に用意してあります。
  - **iPhone でホーム画面に追加したときも、ちゃんとアイコンが出るようになりました**（これまでは画面の写りこみが出ていました）。
  - ブラウザのタブに出る小さいアイコン（ファビコン）も付きました。

### 対応
- `git pull` のみ。データベースの変更はありません。
- **すでにホーム画面に追加している人**は、アイコンが古いままになることがあります。**一度ホーム画面から削除して、追加し直してください**（スマホは一度取得したアイコンを持ち続けるためです）。

---

## 2026-08-10（その4）

### 変更点
- **管理画面に「分析」を追加しました**（サイドバーの「シフト」の次）。**新しく入力する作業は増えません**。すでに入っているデータを見せるだけです。
  - **受付中の枠と未提出者**: 締切が近い順に並び、**まだ出していない人の名前がそのまま出ます**。誰に声をかければいいかがすぐ分かります（脱退した人は出ません）。
  - **メンバーごとの提出率**: 提出率の低い順。締切が過ぎた枠だけを数えるので、これから出せる枠のせいで低く見えることはありません。
  - **役割ごとの充足**: 必要人数に対して実際に何人埋まったか、足りていない枠が何件あるか。
  - **役割の偏り**: 確定した枠での割り当て回数。多い順なので、**下に「まだ一度も入っていない人」が並びます**。

### 対応
- `git pull` のみ。データベースの変更はありません。

---

## 2026-08-10（その3）

### 変更点
- **依存パッケージの脆弱性を解消しました**（警告 7件 → **0件**）。アプリの見た目・操作は何も変わりません。
- Next.js が 16.2.9 → 16.3.0 に上がっています。表示・動作の確認（型チェック・自動チェック・ビルド・未ログイン時の挙動）は済ませてあります。

### 対応
- `git pull` のあと、**`npm install` を実行してください**（パッケージの版が変わっているため）。
- データベースの変更はありません。

---

## 2026-08-10（その2）

### 変更点
- **リンク集を追加しました**（ホーム →「リンク集」）。メンバーが作ってくれたリンク集をアプリに移しました。項目を押すと開いて、リンクボタンと**QRコード**が出ます。**新入生に画面を見せて読み取ってもらえます**。
  - 今出ているのは **山梨大学HP / 新入生応援サイト2026 / 山梨大学生協 / 履修相談会（ポスター）/ 令和8年度学年暦** の5つです。
  - **来場者用アンケート**と**新パ応募フォーム**は、**URLがまだ決まっていないので今は非表示**にしてあります。URLが決まったら教えてください。すぐ出せます。
- **QRコードは画像ではなく、その場で作っています**。リンク先を変えてもQRが古いままになることがないので、**URLを変えるときにQR画像を作り直す必要はありません**。
- ポスター（履修相談会・学年暦）は**オフラインでも見られます**。会場で電波が弱くても開けます。

### 対応
- `git pull` のみ。データベースの変更はありません。
- **リンクを増やす・URLを変える・非表示にする**のは `src/lib/external-links.ts` の1ファイルだけで完結します（ファイルの先頭に手順を書いてあります）。直して push すれば反映されます。

---

## 2026-08-10

### 変更点
- **「商品情報」「目安箱」「アンケート結果」の表示を速くしました**。これまでは**画面を先に出してから中身を取りに行く**作りだったので、開くたびに「読み込み中…」が一瞬出ていました。今回、**中身をサーバー側で用意してから画面を返す**ようにしたので、その待ち時間（サーバーとの往復1回分）が無くなります。**開いた時点で一覧が出ている**状態になります。
- 見た目・操作は今までと同じです。目安箱の投稿フォーム、アンケートの「結果を見る」（開いたときの詳細読み込み）もそのまま動きます。投稿したあとに一覧が更新される動きも変わりません。

### 対応
- `git pull` のみ。データベースの変更はありません。

---

## 2026-08-07（その6）

### 変更点
- **画面の表示を速くしました**。「1つ1つの動作が遅い」という声を受けて調べたところ、**アプリの処理が米国（バージニア）のサーバーで動いていて**、日本からアクセスするたびに太平洋を往復していました。データベース（Supabase）は東京にあるので、往復が二重に発生していた状態です。**処理を東京のサーバーに移しました。**
- あわせて、**同じ問い合わせを1回の表示で何度もしていた箇所をまとめました**（ログイン確認をレイアウトとページで二重に行っていた等）。**ログイン状態のチェックが甘くなることはありません**：脱退やパスワード変更は今までどおり、その場で反映されます。
- **タブを押したあとに画面が固まって見えないよう**、読み込み中の骨組み（グレーの枠）を出すようにしました。

### 対応
- `git pull` のみ。
- （任意）`supabase/migrations/008_add_assignment_user_index.sql` の適用。**検索を速くするための追加だけ**で、既存のデータや列には触れません。**適用しなくてもアプリは正常に動きます**（適用順も問いません）。

---

## 2026-08-07（その5）

### 変更点
- **目安箱で、自分の投稿は必ず自分に見えるようになりました**：これまでは、コアメンバーでない人が「コアだけ」を選んで投稿すると、**投稿した本人にも表示されませんでした**（出したのに消えたように見える状態）。今後は公開範囲に関わらず自分の投稿は一覧に出ます。自分の投稿には**「自分の投稿」バッジ**が付きます（名前を非表示にしていても、本人にだけ分かります）。
- 他の人の見え方は変わりません。**コア限定の投稿は、これまでどおりコアメンバー以外には表示されません**。

### 対応
- `git pull` のみ。

---

## 2026-08-07（その4）

### 変更点
- **上部をすっきりさせました**：**ログアウトボタンは設定画面へ移動**しました（設定 → 一番下の「ログアウト」）。画面上部に残るのは「シフト管理」（押すとホーム）・自分の名前・歯車（設定）だけです。
- **下部タブをさらに大きくしました**：高さ **68px → 80px**、文字も少し大きくしました。ラベルが下辺に詰まって見えないよう、文字の下の余白を厚くしています。

### 対応
- `git pull` のみ。
- **ログアウトの場所が変わりました**。右上の歯車 → 設定画面の一番下です。

---

## 2026-08-07（その3）

### 変更点
- **下部タブを押しやすくしました**：「小さくて押しづらい」との声を受けて、スマホでのタブの高さを **56px → 68px** に広げ、アイコンと文字も大きくしました。押した瞬間に色が変わるようにもしています。右上の**歯車（設定）も指で押せる大きさ**（44px 四方）に広げました。PC では従来どおり詰まった見た目のままです。

### 対応
- `git pull` のみ。

---

## 2026-08-07（その2）

### 変更点
- **自動チェック（CI）を追加しました**：`main` への push と Pull Request のたびに、GitHub Actions で **型チェック → Lint → ビルド**が自動で走ります。壊れた状態に気づかず進むことがなくなります。

### 対応
- `git pull` のみ。
- PR を出すと画面上でチェックの結果が見えます。**赤（失敗）のときはマージ前に直してください**。手元で同じことを確かめるには `npm run typecheck` → `npm run lint` → `npm run build` を実行します。
- 本番の環境変数（Supabase / LINE のキー）は CI では使いません。設定しないでください。

---

## 2026-08-07

### 変更点
- **スタッフ画面のナビを整理しました**（タブが6つあって1つ1つが小さい、という声を受けての変更です）。
  - **下部のタブは4つ**（シフト / 商品 / 目安箱 / アンケート）になり、**アイコン付き**で現在地が分かりやすくなりました。
  - **「設定」は画面右上の歯車アイコン**に移りました。
  - **左上の「シフト管理」を押すとホームに戻れます**。
  - **「役割」タブはシフト画面に統合**しました。シフト画面の上部で **「希望を出す」/「確定した役割」** を切り替えます（「希望を出す」には未回答の件数バッジが出ます）。旧 URL `/dashboard/my-roles` は自動で転送されるので、ブックマークもそのまま使えます。
- **ホーム画面が「今なにをすればいいか」の要約になりました**：未提出のシフト希望（期限つき・あるときだけ警告表示）、次のシフトと自分の役割、新着のアンケート結果が出ます。

### 対応
- `git pull` のみ。DB の変更はありません。

---

## 2026-08-06（その2）

### 変更点
- **当日アンケート結果の共有を追加しました**：当日にお客さんからいただいたアンケートの結果を、運営が登録してスタッフへ共有できます。
  - スタッフ側：**「アンケート」タブ**（`/dashboard/surveys`）で公開された結果を見られます。項目ごとの数値・自由記述に加え、**同じ項目の過去の回との推移（前回比）**も表示されます。
  - 管理側：管理画面の**「アンケート」**で、結果の登録 → 項目ごとの値を入力 → **「保存して公開」**の流れです。公開するまでスタッフには見えません（下書き）。公開範囲は**全スタッフ / コアメンバーのみ**を選べます。
  - **質問項目は管理画面で自由に登録できます**（一覧画面の「アンケートの項目設定」）。種類は「数値（平均・割合・件数、単位つき）」と「自由記述」の2つ。選択肢ごとの割合は、選択肢ごとに項目を分けて % で入れてください。項目は回をまたいで共通なので、同じ項目を使い続けると推移が見られます。
  - すでに結果で使われている項目は削除できません（過去の結果が欠けるため）。使わなくなった項目は「非表示にする」でその後の入力欄から外せます。

### 対応
- `git pull`。
- **⚠️ 管理者作業（先に必要）**：Supabase の SQL Editor で **`supabase/migrations/007_create_surveys.sql` を実行**してください。**マイグレーションを先に、コードの反映を後に**行ってください（逆順だとアンケート画面が一時的にエラーになります。他の機能には影響しません）。
- アンケートの質問項目は初期状態では空です。運用開始時に管理画面から登録してください。

---

## 2026-08-06

### 変更点
- **シフト枠の時刻を 15 分刻みで選べるように**：これまでの時刻入力は 1 分刻みで、狙った時刻に合わせるのが手間でした。管理画面の**シフト枠の新規作成**と**「日付・時間・備考を編集」**の開始／終了を、**15 分刻みのプルダウン（00:00〜23:45）** に変更しました。
- 既に登録済みで 15 分刻みでない時刻（例 10:05）は、そのままプルダウンの選択肢に出るので、編集しても勝手に丸められません。

### 対応
- `git pull` のみ。

---

## 2026-07-15

### 変更点
- **役割変更が「確定して共有」で反映されないバグを修正**：役割割り振りのプルダウンで役割を変えても、「手動調整を保存」を押さずに「確定して共有」すると、変更前（自動割り振り時）の役割のまま公開・通知されていました。今後は「確定して共有」時に画面上の役割を自動保存してから公開するようにしました。

### 対応
- `git pull` のみ。

---

## 2026-07-13（その3）

### 変更点
- **LINE連携後に「友だち追加」へ誘導**：通知は公式アカウントを友だち追加した人にしか届かないため、設定画面で友だち状態を確認し、**未追加なら「LINEで友だち追加」ボタン**を表示するようにしました（追加済みなら「友だち追加済み ✓」）。

### 対応
- `git pull` のみ。
- **LINE Developers Console 側の設定が必要**（管理者作業）：
  - LINE ログインチャネル → **「Linked LINE Official Account」に通知用の公式アカウントをリンク**、**「Add friend option（友だち追加オプション）」を On**（ログイン時に友だち追加案内が出る＋`getFriendship` が使えるようになる）。
- **⚠️ Vercel の環境変数（Production）に `NEXT_PUBLIC_LINE_ADD_FRIEND_URL` を追加**（公式アカウントの友だち追加 URL。例 `https://lin.ee/xxxx`）。**未設定なら友だち追加案内は表示されません**。`NEXT_PUBLIC_` はビルド時に埋め込まれるため、設定後に**再デプロイが必要**。
- 本番（Vercel）は `main` への push で自動再デプロイ。

## 2026-07-13（その2）

### 変更点
- **シフト希望の提出リマインドを自動配信**（LINE）：提出期限が**当日**のシフトについて、**毎朝 8:00（JST）**に、まだ希望を出していない対象者へ LINE で自動リマインドを送ります。
  - 1人が複数の未提出シフトを持つ場合は**1通にまとめて**送信。
  - LINE 連携済みの人のみが対象（未連携者はスキップ）。同じ日に二重送信しないようにしています。
  - 送信結果は管理画面の「通知ログ」（種別 `shift_reminder`）で確認できます。

### 対応
- `git pull` のみ。
- **⚠️ Vercel の環境変数（Production）に `CRON_SECRET` の追加が必要**（長いランダム文字列）。未設定だとリマインドは実行されません（安全側で 401 no-op）。設定後に再デプロイで有効化。
- 定期実行は `vercel.json` の Cron（`0 23 * * *` UTC = 8:00 JST）で自動。
- 本番（Vercel）は `main` への push で自動再デプロイ。

## 2026-07-13

### 変更点
- **LINE 連携（設定画面）の不具合を修正**：外部ブラウザで「LINEと連携する」を押すと、LINE ログインは通るのに**連携済みにならない**（エラーも出ない）問題を解消。
  - ログインから戻ってきたときに**自動で連携を完了**するようにした（ボタンを押し直す必要なし）。
  - 連携に失敗した場合は**エラーメッセージを表示**するようにした。

### 対応
- `git pull` のみ。環境変数の変更・追加インストールなし。
- **LINE Developers Console 側の設定変更が必要**（管理者作業）：LINE ログインチャネルの LIFF の **Endpoint URL** を `https://adviser-shift-app.vercel.app/dashboard/settings` に変更し、Callback URL に同 URL（±末尾スラッシュ）が入っていることを確認。
- 本番（Vercel）は `main` への push で自動再デプロイ。

## 2026-07-01（その2）

### 変更点
- **パスワードのリセット／変更機能を追加**（パスワードを忘れたときの復旧手段）:
  - **管理者**: メンバー管理の各メンバーに「**PWリセット**」ボタンを追加。押すと**仮パスワードが1回だけ表示**されるので、本人に伝える（画面を閉じると再表示できません）。リセットするとロックも解除されます。
  - **本人**: 設定画面に「**パスワード変更**」を追加。仮パスワードで入った人が、現在のパスワードを入れて自分の好きなパスワードに変更できます（8文字以上）。
  - パスワードは引き続きハッシュ化して保存（管理者でも中身は見られません）。
- **セッション失効（token バージョニング）**: パスワードを**リセットするとその人の全端末**、**本人が変更すると他端末**が自動でログアウトされます（操作中の端末はそのまま）。Cookie を盗まれても、パスワード変更で締め出せます。

### 対応
- `git pull` のみ。環境変数の変更・追加インストールなし。
- **DB マイグレーション `006_add_token_version.sql` は本番 DB に適用済み**（共有 DB のため各自の作業は不要）。
- 本番（Vercel）は `main` への push で自動再デプロイ。
- ℹ️ この更新自体では既存のログインは維持されます（一斉ログアウトは起きません）。

## 2026-07-01

### 変更点
- **ログイン失敗カウンタのアトミック化**（セキュリティ堅牢化。通常利用の挙動は不変）:
  - ログイン失敗回数の加算を DB 関数側で**行ロックして直列化**するように変更。これまでは失敗リクエストを**同時に大量送信**するとカウントが正しく増えず、5回ロックを回避できる余地があった（総当たり対策の穴）。ユーザー・管理ログインの両方に対応。
  - 失敗時の残り回数表示・15分ロック・15分ウィンドウなど、**利用者から見える挙動は従来どおり**。

### 対応
- `git pull` のみ。**環境変数の変更・追加インストールなし**。
- **DB マイグレーション `005_atomic_login_failure.sql` は本番 DB に適用済み**（共有 DB のため各自の作業は不要）。
- 本番（Vercel）は `main` への push で自動再デプロイ。

## 2026-06-29（その3）

### 変更点
- **コードレビュー＆セキュリティ点検にもとづく堅牢化**（致命的な脆弱性は無し。挙動は不変）:
  - シフト枠作成・自動割り振り・手動調整・やり直しで、付随データの書き込み失敗時に**不完全な状態が残らない**よう、エラー検査と取り消し（補償削除）を追加。
  - 全 API の動的 URL（`/[id]`）に **UUID 形式チェック**を追加（不正なIDは一律「見つかりません」）。
  - 本番の **Content-Security-Policy から `unsafe-eval` を除外**（開発時のみ許可。XSS 時の攻撃面を縮小）。
  - JWT の署名・検証アルゴリズムを **HS256 に固定**（多層防御）。

### 対応
- `git pull` のみ。**DB スキーマ変更・環境変数の変更・追加インストールなし**。
- 本番（Vercel）は `main` への push で自動再デプロイ。
- ⚠️ CSP 変更があるため、デプロイ後にプレビュー/本番で**画面が正常に描画されること**（白画面でない・主要操作可）を一度確認してください。

## 2026-06-29（その2）

### 変更点
- **依存パッケージの脆弱性修正**: `npm audit fix`（破壊的変更なし）で **high 1件（form-data の CRLF インジェクション）** と low 1件（esbuild・開発時/Windows のみ）を解消。`package-lock.json` のみ更新（アプリの挙動・機能変更なし）。
  - 残る 2件（moderate）は **Next.js 本体が同梱する postcss** によるもので、修正には Next のダウングレード（破壊的）が必要なため従来どおり受容（Next 本体のパッチ待ち）。

### 対応
- `git pull` のみ。`npm install`（lock 更新を反映）を推奨。環境変数の変更なし。
- 本番（Vercel）は `main` への push で自動再デプロイ。

## 2026-06-22（その4）

### 変更点
- **目安箱に「運営からの返答」を追加**: 管理者が各投稿に**返答**を書けるようになりました（1投稿に1件・あとから編集／削除可）。
  - 返答を**保存すると自動で「対応済み」**になります。
  - 返答はその投稿が見える人に表示されます（全体公開なら全員、コア限定ならコアメンバー）。スタッフ画面に「運営からの返答」として表示。
  - 管理画面のメンバー…ではなく**目安箱**の各投稿カードから「返答する／編集／削除」。

### 対応
- ✅ **DB スキーマ変更（適用済み）**: `supabase/migrations/004_add_suggestion_reply.sql`（`suggestions` に `admin_reply`・`replied_at` カラムを追加）を **2026-06-29 に本番 Supabase へ適用済み**。本番 DB は共有のため、各自での適用は不要です。
- 追加インストール・環境変数の変更は不要。
- 本番（Vercel）は `main` へのマージで自動再デプロイ済み。返答機能はフル稼働しています。

## 2026-06-22（その3）

### 変更点
- **役割の設定を管理者専用に変更**: これまで一般ユーザーが自分の設定画面で当日用・研修用の役割を自由に変更できましたが、**管理者だけが設定できる**ようにしました。
  - スタッフの設定画面では役割は**閲覧のみ**になり、「役割は管理者が設定します」と案内を表示。
  - 管理画面のメンバー一覧に**「役割編集」**を追加（行を展開してチェックボックスで設定）。
  - 背景: 目安箱の「コアだけ」公開がコアメンバー役割に依存するため、本人が役割を自由に変えられると不適切な閲覧につながるリスクを防止。

### 対応
- `git pull` のみ。**DB スキーマ変更・環境変数の変更なし**、追加インストール不要。
- 本番（Vercel）は `main` へのマージで自動再デプロイ。
- 既存ユーザーの設定済み役割はそのまま残ります（以後は本人変更不可・管理者のみ）。

## 2026-06-22（その2）

### 変更点
- **目安箱機能を追加**: スタッフが運営へ**質問・意見・相談**を投稿できる目安箱を新設。
  - 投稿フォーム：**種類**（商品/業務/人間関係/このアプリ/その他）・**区分**（質問/意見/相談/その他）・**名前**（表示／非表示）・**公開範囲**（全体／コアだけ）・**内容**。
  - **公開範囲「コアだけ」** は、研修役割に「コアメンバー」を持つスタッフだけが閲覧可能（管理者は全件閲覧可）。
  - **名前「非表示」** は画面に投稿者名を出しません（管理画面でも非表示）。
  - スタッフ画面：自分が見られる投稿の一覧、管理画面：全投稿の閲覧＋**未対応／対応済みのステータス管理**＋削除。
  - 下ナビ・管理サイドバー・ダッシュボードに「目安箱」を追加。

### 対応
- ⚠️ **DB スキーマ変更あり**: `supabase/migrations/003_create_suggestions.sql` を**本番 Supabase に適用してください**（`suggestions` テーブルを新規作成）。**未適用だと目安箱がエラーになります**。
- 追加インストール・環境変数の変更は不要。
- 本番（Vercel）は `main` へのマージで自動再デプロイ。**migration 適用後にデプロイ**するのが安全。
- 備考: 「名前 非表示」でも投稿者の記録（user_id）はサーバーに保存され、不適切投稿時に運営が辿れる前提です（厳密な完全匿名ではありません）。

## 2026-06-22

### 変更点
- **在庫の更新日を表示**: 「在庫数がいつ更新されたか分からず、古い在庫を最新と思って案内してしまう」リスクへの対処。
  - スタッフの商品画面・管理の商品テーブルに「在庫更新: 6/22 14:30（3時間前）」のように**在庫数を最後に変えた日時**を表示。
  - 更新から **24 時間以上**経過した在庫は琥珀色＋「⚠️ 古い情報の可能性」で注意喚起。
  - 在庫の鮮度は名前・カテゴリ等の編集では更新されず、**在庫数を変えたときだけ**更新される（専用カラム `stock_updated_at`）。

### 対応
- ⚠️ **DB スキーマ変更あり**: `supabase/migrations/002_add_stock_updated_at.sql` を**本番 Supabase に適用してください**（`products` に `stock_updated_at` 列を追加。既存行は `updated_at` で初期化）。**未適用だと商品 API がエラーになります**。
- 追加インストール・環境変数の変更は不要。
- 本番（Vercel）は `main` へのマージで自動再デプロイ。**migration 適用後にデプロイ**するのが安全（先に列を用意 → コード配信）。

## 2026-06-12

### 変更点
- **スマホ下ナビの高さ改善**: 「シフト/役割/商品/設定」のボタンが縦に窮屈だったのを解消。タップ領域を拡大し、iPhone のホームバー（セーフエリア）にも対応。
- **シフトの並び順を改善**（希望提出画面・役割確認画面）:
  - 同じ日付内は**開始時刻の早い順**で表示。
  - **過ぎたシフトは下**へ、**次のシフトが一番上**に来るよう並べ替え（JST で日付が変わったら「過去」扱い）。
- PWA アイコン更新／`.vercel` を `.gitignore` に追加。

### 対応
- `git pull` のみ。**環境変数・DB スキーマの変更なし**、追加インストール不要。
- 本番（Vercel）は `main` へのマージで自動再デプロイ済み。
